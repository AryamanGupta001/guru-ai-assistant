# Global Configuration Settings for GURU

# Example: Application settings
APP_NAME = "GURU AI Assistant"
DEBUG_MODE = True  # Set to False in production

# Example: API configurations (if not purely in .env or module-specific)
# SOME_OTHER_SERVICE_URL = "https://api.example.com/v1"

# Example: Paths (though usually derived dynamically)
# LOG_FILE_PATH = "logs/app.log"

# Add other global configurations as needed
print(f"{APP_NAME} configuration loaded.")
